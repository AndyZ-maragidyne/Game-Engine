To compile the program, all that needs to be done is open the solution file through MS Visual Studio and run the program through the MSVC compiler. All SDL include files and libraries are included in the zip and are accessed through relative file paths.